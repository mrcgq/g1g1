# Phantom Server v2.0 配置

# 监听地址
listen: ":54321"

# 预共享密钥 (Base64 编码, 32 字节)
# 生成方法: openssl rand -base64 32
psk: "YOUR_PSK_HERE"

# 时间窗口 (秒)
# 用于 TSKD 密钥派生和防重放
time_window: 30

# 日志级别: debug, info, error
log_level: "info"
